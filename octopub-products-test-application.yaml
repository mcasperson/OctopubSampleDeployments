apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: octopub-products-test-application
  annotations:
    codefresh.io/product: octopub-products
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  project: default
  destination:
    name: in-cluster
    namespace: octopub-test
  source:
    path: octopub/products/test
    repoURL: https://github.com/mcasperson/OctopubSampleDeployments.git
    targetRevision: main
    helm:
      valueFiles:
        - values.yaml
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
      allowEmpty: false
    syncOptions:
      - PrunePropagationPolicy=foreground
      - Replace=false
      - PruneLast=false
      - Validate=true
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=false
      - ServerSideApply=true
      - RespectIgnoreDifferences=false